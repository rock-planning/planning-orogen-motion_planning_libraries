# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(global_path_plannerTaskLib)
ADD_LIBRARY(${GLOBAL_PATH_PLANNER_TASKLIB_NAME} SHARED 
    ${GLOBAL_PATH_PLANNER_TASKLIB_SOURCES})
add_dependencies(${GLOBAL_PATH_PLANNER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${GLOBAL_PATH_PLANNER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${GLOBAL_PATH_PLANNER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${GLOBAL_PATH_PLANNER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${GLOBAL_PATH_PLANNER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${GLOBAL_PATH_PLANNER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${GLOBAL_PATH_PLANNER_TASKLIB_HEADERS}
    DESTINATION include/orocos/global_path_planner)

